# Simple React Vite App

Small demo app that fetches posts from a public API and shows loading/error states. Built with Vite + React.

Quick start

```bash
npm install
npm run dev
```

Open the local URL printed by Vite (usually http://localhost:5173).

Files

- `src/` — app source. `src/components/PostList.jsx` handles fetching and UI.
